# Modify and add this to your .zshrc or .bashrc file
# Note that multi-file kubeconfig is not supported by kubectx, so you need to use 'kubectl config'
export RRIV_PROJECT_PATH="$HOME/path/to/rriv-cloud"
export K8S_DIRECTORY="$RRIV_PROJECT_PATH/02-tf/modules/do/k8s-cluster/kubeconfig"
export KUBECONFIG="$K8S_DIRECTORY/vault-dev.yaml"\
:"$K8S_DIRECTORY/rriv-dev.yaml"\
:"$K8S_DIRECTORY/vault-staging.yaml"\
:"$K8S_DIRECTORY/rriv-staging.yaml"\
:"$K8S_DIRECTORY/vault-prod.yaml"\
:"$K8S_DIRECTORY/rriv-prod.yaml"

# Get these from your DigitalOcean account for each Team.
export MGMT_DO_TOKEN="your_do_token"
export MGMT_SPACES_ACCESS_KEY_ID="your_spaces_access_key_id"
export MGMT_SPACES_SECRET_ACCESS_KEY="your_spaces_secret_access_key"
export DEV_DO_TOKEN="your_do_token"
export DEV_SPACES_ACCESS_KEY_ID="your_spaces_access_key_id"
export DEV_SPACES_SECRET_ACCESS_KEY="your_spaces_secret_access_key"
export STAGING_DO_TOKEN="your_do_token"
export STAGING_SPACES_ACCESS_KEY_ID="your_spaces_access_key_id"
export STAGING_SPACES_SECRET_ACCESS_KEY="your_spaces_secret_access_key"
export PROD_DO_TOKEN="your_do_token"
export PROD_SPACES_ACCESS_KEY_ID="your_spaces_access_key_id"
export PROD_SPACES_SECRET_ACCESS_KEY="your_spaces_secret_access_key"

# Terraform secrets should ***NOT*** be stored in this repo!!!
# AI agents like Copilot can read anything you open in your editor.
# Put them in a folder outside of this repo, e.g. ../rriv-cloud-secrets
# Add this line to alias the terraform command so it can use the secrets:
tf() {
  terraform "$@" -var-file="${RRIV_PROJECT_PATH}/../rriv-cloud-secrets/secrets-${ENV}.tfvars"
}
# Directory structure:
#
# rriv-cloud/         # this repo
# rriv-cloud-secrets/ # edit these files in a basic text editor
# ├── secrets-dev.tfvars
# ├── secrets-staging.tfvars
# └── secrets-prod.tfvars
#
# Example usage:
# export ENV=dev && tf plan